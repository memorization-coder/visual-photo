# Contracts

This package is the durable TypeScript source of truth for DTOs, enums, RPC request/response payloads, and AI schemas. Future native clients should mirror these contracts rather than redefining business shapes ad hoc.

Product-behavior contracts such as UX rules, route intent, demo scope, and feature acceptance criteria live separately in `docs/contracts`.
