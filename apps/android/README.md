# Android Placeholder

This directory is reserved for the future native Android client. It will consume the shared contracts, shared domain rules, and the same Supabase RPC and Edge Function contracts defined in Contract 0.

